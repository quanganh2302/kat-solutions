# Add server-side integration variables only when an approved integration is implemented.

# Origin of the VSA demo deployment (its own Vercel project). Enables the
# /demo/vsa multi-zone proxy in next.config.ts. Example:
# VSA_DEMO_ORIGIN="https://vsa-demo.vercel.app"
